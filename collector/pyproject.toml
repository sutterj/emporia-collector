[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["."]
omit = ["tests/*", "test_auth.py"]

[tool.coverage.report]
show_missing = true
fail_under = 75
